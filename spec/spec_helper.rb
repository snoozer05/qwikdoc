# -*- coding: utf-8 -*-
$:.unshift File.join("..", "lib", "qwik_doc")

Bundler.require :test if defined?(Bundler)

RSpec.configure do |spec_config|
  spec_config.before(:each) do
  end

  spec_config.after(:each) do
  end
end
